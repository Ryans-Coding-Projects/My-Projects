#This program converts a users input number to the binary equivalent

number_to_binary = int(input("Enter Number to Convert to Binary Code: "))

print(f"{number_to_binary} in Binary Code is",bin(number_to_binary))
